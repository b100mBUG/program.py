# Let's get serious with python. lol
# First, write anything,not comments, just write a python program, even if it's incorrect, just write it.
# For example, let's do that teacher's assignment. We were to write a python program to compute mean by calling a function that does that...


# first, define the function that calculates mean...

# Then, write the main function, and then, request user inputs, then, call the function, to yield the mean, and print it. 
